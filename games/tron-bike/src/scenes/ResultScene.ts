// ResultScene is handled inline in GameScene via showMatchEndButtons.
// This stub exists for future expansion (leaderboard, ELO display, etc.)
import Phaser from 'phaser';
export class ResultScene extends Phaser.Scene {
  constructor() { super('ResultScene'); }
}
